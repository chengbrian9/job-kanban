{
  "name": "job-kanban",
  "version": "1.0.0",
  "description": "Job Kanban Board Application",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "python app.py",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
